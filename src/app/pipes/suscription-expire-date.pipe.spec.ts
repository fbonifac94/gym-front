import { SuscriptionExpireDatePipe } from './suscription-expire-date.pipe';

describe('SuscriptionExpireDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SuscriptionExpireDatePipe();
    expect(pipe).toBeTruthy();
  });
});
