import { SuscriptionTimeUnitsPipe } from './suscription-time-units.pipe';

describe('SuscriptionTimeUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new SuscriptionTimeUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
